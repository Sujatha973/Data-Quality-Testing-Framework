name: 'dq_project'
version: '1.0.0'
config-version: 2

profile: 'dq_project'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dq_project:
    staging:
      +materialized: view
      +schema: staging
